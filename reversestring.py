# Reversing a string
  #It is a way to extract a portion of a sequence (like string or list) 
  # #using the syntax [start:end:step]. It’s very useful for accessing subsets or reversing data.”
text = "Python"
print("Reversed:", text[::-1])   #slicing concept is used here
